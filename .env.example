APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:kgk/4DW1vEVy7aEvet5FPp5un6PIGe/so8H0mvoUtW0=

APP_NAME="InvoiceShelf"
APP_TIMEZONE=UTC
APP_URL=
APP_LOCALE=en

# Optional: public extensions catalog JSON (default: InvoiceShelf/extensions on GitHub).
# Do not set this to an empty value — leave the line commented out to use the default.
# INVOICESHELF_EXTENSIONS_MANIFEST_URL=https://raw.githubusercontent.com/InvoiceShelf/extensions/main/extensions.json

# Optional: PDF invoice/estimate template catalog JSON (default: InvoiceShelf/extensions templates.json).
# INVOICESHELF_TEMPLATES_MANIFEST_URL=https://raw.githubusercontent.com/InvoiceShelf/extensions/main/templates.json

DB_CONNECTION=sqlite
DB_HOST=
DB_PORT=
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=

SESSION_DOMAIN=null
SANCTUM_STATEFUL_DOMAIN=
TRUSTED_PROXIES="*"

# Dompdf: keep false so untrusted HTML in PDF notes cannot trigger outbound requests (SSRF).
# Set true only if you fully trust all PDF HTML and need remote images/CSS.
DOMPDF_ENABLE_REMOTE=false
